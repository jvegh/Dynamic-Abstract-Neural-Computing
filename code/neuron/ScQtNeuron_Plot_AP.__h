
/************************************************************************************************************
**                                                                                                         **
**  This is the example code for QCustomPlot.                                                              **
**                                                                                                         **                                                                                                         **
*************************************************************************************************************/

#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QMainWindow>
#include <QTimer>
#include "../../3rdParty/qcustomplot/include/qcustomplot.h"

class ScQtNeuron_MainWindow;

class ScQtNeuronVoltagePlot : public QCustomPlot
{
  Q_OBJECT
  
public:
  explicit ScQtNeuronVoltagePlot(ScQtNeuron_MainWindow *parent = 0
                                   );
  ~ScQtNeuronVoltagePlot();
  
  void setupRealtimeDataDemo(void);
//  void setupParametricCurveDemo(QCustomPlot *customPlot);

  void setupPlayground();

  void ProcessLine(QString line);
  void GetData(QString fileName);
  QStringList first,second;
  uint32_t index, lastindex;

  QVector<double> Time, Voltage;

private slots:
  void realtimeDataSlot();
  void screenShot();
  
private:
  QString demoName;
  QTimer dataTimer;
  QCPItemTracer *itemDemoPhaseTracer;
//  int currentDemoIndex;
  ScQtNeuron_MainWindow *m_parent;
};
#endif // MAINWINDOW_H
